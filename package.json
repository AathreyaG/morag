{
    "name": "morag",
    "version": "0.0.1",
    "description": "Module to perform SQL operations using canned SQL Statements. This takes care of creating connection pool, rollback, commit so the consumer does not worry about the operations.",
    "main": "index.js",
    "scripts": {
        "test": "nyc --reporter=lcov mocha --timeout 10000"
    },
    "author": "STS Core Team",
    "license": "ISC",
    "dependencies": {
        "bluebird": "^3.5.4",
        "express": "^4.16.4",
        "http-status-codes": "^1.3.2",
        "lodash": "^4.17.11",
        "pg": "^7.11.0"
    },
    "devDependencies": {
        "chai": "^4.2.0",
        "mocha": "^6.1.4",
        "nyc": "^14.0.0"
    }
}